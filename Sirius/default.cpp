#include <bits/stdc++.h>

using namespace std;

// #define DEBUG

void solve() {
  // Some code
  cout << 0;
}

int main() {
  #ifdef DEBUG
  // Ввод с фай
  #endif

  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t = 1;

  while (t--) {
    solve();
  }
}
