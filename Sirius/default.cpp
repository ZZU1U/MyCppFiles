#include <bits/stdc++.h>

using namespace std;

// #define DEBUG

void solve() {
  // Some code
  cout << 0;
}

int main() {
#ifdef LOCAL
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
#endif

  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int t = 1;

  while (t--) {
    solve();
  }
}
