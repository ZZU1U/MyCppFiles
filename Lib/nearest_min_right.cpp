#include <iostream>
#include <stack>

using namespace std;

// There's another example of solving this problem

vector<int> near_min_right(vector<int> vec, int n) {
  vector<int> ans(n);
  stack<int> inds;

  for (int i = 0; i < n; i++) {
    
  }
}

